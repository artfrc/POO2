#include <iostream>
#include "Character.h"

class Character {
    private:
        string name;
        int id;

    public:
        Character(string name, int id) : name(name), id(id){}


        string get_name() {
            return this->name;
        }

        int get_id() {
            return this->id;
        }


};
